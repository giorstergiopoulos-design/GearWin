<UserControl x:Class="OptimizerWpf.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <!--
        Stage 1 vertical slice: the Home tab, matching Optimizer.ps1's $tabHome (Add-CustomTabPage
        "TabHome") layout - CPU/RAM/Disk metric cards, Health Score card, Disk Analysis card.
        CPU/RAM values below are wired to live data (see HomeView.xaml.cs); Disk %/free-space,
        Health Score, and Disk Analysis are still static placeholders - the next increment is
        porting Get-SystemHealthScore's logic and the disk-usage WMI queries.
    -->
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,8,0,24">

            <TextBlock Text="Παρακολούθηση Απόδοσης Συστήματος" Style="{StaticResource CardTitleStyle}" FontSize="16" Margin="0,0,0,12"/>

            <Grid Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="16"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- CPU -->
                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Width="56" Height="56" CornerRadius="28" Margin="0,0,0,10">
                            <Border.Background>
                                <RadialGradientBrush GradientOrigin="0.3,0.3" Center="0.5,0.5" RadiusX="0.9" RadiusY="0.9">
                                    <GradientStop Color="#FF6EC6FF" Offset="0"/>
                                    <GradientStop Color="#FF2196F3" Offset="0.6"/>
                                    <GradientStop Color="#FF0D47A1" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                            <TextBlock Text="⚙" FontSize="24" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Επεξεργαστής (CPU)" Style="{StaticResource CardTitleStyle}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtCpuPercent" Text="--%" FontSize="26" FontWeight="Bold" Foreground="#FF2196F3" HorizontalAlignment="Center" Margin="0,6,0,8"/>
                        <ProgressBar x:Name="BarCpu" Height="6" Width="180" Minimum="0" Maximum="100" Foreground="#FF2196F3"/>
                    </StackPanel>
                </Border>

                <!-- RAM -->
                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <Border Width="56" Height="56" CornerRadius="28" Margin="0,0,0,10">
                            <Border.Background>
                                <RadialGradientBrush GradientOrigin="0.3,0.3" Center="0.5,0.5" RadiusX="0.9" RadiusY="0.9">
                                    <GradientStop Color="#FF8BEA9E" Offset="0"/>
                                    <GradientStop Color="#FF4CAF50" Offset="0.6"/>
                                    <GradientStop Color="#FF1B5E20" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>
                            <TextBlock Text="&#x1F4BE;" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Μνήμη (RAM)" Style="{StaticResource CardTitleStyle}" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtRamPercent" Text="--%" FontSize="26" FontWeight="Bold" Foreground="#FF4CAF50" HorizontalAlignment="Center" Margin="0,6,0,8"/>
                        <ProgressBar x:Name="BarRam" Height="6" Width="180" Minimum="0" Maximum="100" Foreground="#FF4CAF50"/>
                        <TextBlock x:Name="TxtRamDetail" Text="-- / -- GB" Style="{StaticResource SubTextStyle}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Disk - ΔΙΟΡΘΩΣΗ (ρητό αίτημα χρήστη): αντί για ComboBox, βελάκια αριστερά/δεξιά στα
                     άκρα του πλακιδίου για εναλλαγή δίσκου· το εικονίδιο/χρώμα αλλάζει ανάλογα με τον
                     τύπο (HDD/SSD/USB - βλ. Services/DriveTypeService.cs) μέσω του UpdateDiskIcon
                     στο code-behind. -->
                <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                    <Grid>
                        <StackPanel HorizontalAlignment="Center">
                            <Border x:Name="BorderDiskIcon" Width="56" Height="56" CornerRadius="28" Margin="0,0,0,10">
                                <TextBlock x:Name="TxtDiskIcon" Text="&#x1F4BF;" FontSize="22" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock x:Name="TxtDriveLabel" Text="C:" Style="{StaticResource CardTitleStyle}" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtDriveKind" Text="" Style="{StaticResource SubTextStyle}" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtDiskPercent" Text="--%" FontSize="26" FontWeight="Bold" Foreground="#FFFF9800" HorizontalAlignment="Center" Margin="0,6,0,8"/>
                            <ProgressBar x:Name="BarDisk" Height="6" Width="180" Minimum="0" Maximum="100" Foreground="#FFFF9800"/>
                            <TextBlock x:Name="TxtDiskDetail" Text="-- GB ελεύθερα από -- GB" Style="{StaticResource SubTextStyle}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>
                        <Button x:Name="BtnDrivePrev" Content="&#x2039;" FontSize="20" FontWeight="Bold" Width="28" Height="40"
                                HorizontalAlignment="Left" VerticalAlignment="Center" Style="{StaticResource FlatButtonStyle}" Click="BtnDrivePrev_Click"/>
                        <Button x:Name="BtnDriveNext" Content="&#x203A;" FontSize="20" FontWeight="Bold" Width="28" Height="40"
                                HorizontalAlignment="Right" VerticalAlignment="Center" Style="{StaticResource FlatButtonStyle}" Click="BtnDriveNext_Click"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Health Score -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="140"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="Βαθμολογία Υγείας Συστήματος" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,12"/>

                    <StackPanel Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock x:Name="TxtHealthScore" Text="--" FontSize="38" FontWeight="Bold" Foreground="#FF4CAF50" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtHealthLabel" Text="Έλεγχος..." FontStyle="Italic" Foreground="#FF4CAF50" HorizontalAlignment="Center"/>
                    </StackPanel>

                    <Border Grid.Row="1" Grid.Column="1" Background="{DynamicResource MainBgBrush}" CornerRadius="6" MinHeight="90" MaxHeight="140" Padding="10">
                        <ScrollViewer VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="ListHealthIssues">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                            <Ellipse Width="8" Height="8" Fill="{Binding DotColor}" Margin="0,4,8,0" VerticalAlignment="Top"/>
                                            <TextBlock Text="{Binding Title}" Style="{StaticResource SubTextStyle}" Foreground="{DynamicResource TextBrush}" Width="720"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Border>

                    <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" Margin="0,10,0,0">
                        <Button Content="Διόρθωση Όλων" Style="{StaticResource AccentButtonStyle}" Width="150" Margin="0,0,10,0" Click="BtnFixAllHealth_Click"/>
                        <Button Content="Ανανέωση" Style="{StaticResource FlatButtonStyle}" Width="150" Click="BtnRefreshHealth_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Disk Analysis -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Ανάλυση Χώρου Δίσκου ανά Κατηγορία" Style="{StaticResource CardTitleStyle}" Margin="0,0,0,6"/>
                            <TextBlock x:Name="TxtDiskAnalysisStatus" Text="Πατήστε 'Ανάλυση' για κατανομή χώρου ανά κατηγορία (επιλεγμένος δίσκος από την κάρτα Δίσκου παραπάνω)." Style="{StaticResource SubTextStyle}"/>
                        </StackPanel>
                        <Button Grid.Column="1" Content="Ανάλυση" Style="{StaticResource AccentButtonStyle}" Width="130" VerticalAlignment="Center" Click="BtnAnalyzeDisk_Click"/>
                    </Grid>

                    <ItemsControl x:Name="ListDiskCategories" Margin="0,12,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,0,0,6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="70"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="{DynamicResource TextBrush}" VerticalAlignment="Center"/>
                                    <Border Grid.Column="1" Height="10" Background="{DynamicResource BtnDefaultBrush}" CornerRadius="5" Margin="8,0" VerticalAlignment="Center">
                                        <Border HorizontalAlignment="Left" Width="{Binding BarWidth}" Height="10" Background="{Binding BarColor}" CornerRadius="5"/>
                                    </Border>
                                    <TextBlock Grid.Column="2" Text="{Binding SizeText}" Style="{StaticResource SubTextStyle}" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>
