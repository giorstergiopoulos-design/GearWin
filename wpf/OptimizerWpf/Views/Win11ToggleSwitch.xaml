<UserControl x:Class="OptimizerWpf.Views.Win11ToggleSwitch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="46" d:DesignHeight="24">
    <!-- Ρητό αίτημα χρήστη: "δεν έχουν το κουμπί των win11" - πραγματικός στρογγυλεμένος διακόπτης
         (track + ολισθαίνων κύκλος), αντί για ζεύγος κουμπιών Ενεργοποίηση/Απενεργοποίηση, στο
         ViVeToolWindow. -->
    <ToggleButton x:Name="Toggle" Width="46" Height="24" Cursor="Hand" Click="Toggle_Click">
        <ToggleButton.Template>
            <ControlTemplate TargetType="ToggleButton">
                <Border x:Name="Track" CornerRadius="12" Background="{DynamicResource BtnDefaultBrush}" BorderBrush="{DynamicResource CardBorderBrush}" BorderThickness="1">
                    <Border x:Name="Thumb" Width="18" Height="18" CornerRadius="9" Background="White" HorizontalAlignment="Left" Margin="2,0,0,0"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsChecked" Value="True">
                        <Setter TargetName="Track" Property="Background" Value="{DynamicResource AccentBrush}"/>
                        <Setter TargetName="Track" Property="BorderThickness" Value="0"/>
                        <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                        <Setter TargetName="Thumb" Property="Margin" Value="0,0,2,0"/>
                    </Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate>
        </ToggleButton.Template>
    </ToggleButton>
</UserControl>
