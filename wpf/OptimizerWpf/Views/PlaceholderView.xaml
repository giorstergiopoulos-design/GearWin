<UserControl x:Class="OptimizerWpf.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <!-- Generic stand-in for a tab not yet ported from Optimizer.ps1 - set Text in code-behind
         (see PlaceholderView.xaml.cs) so each unported tab still shows which one it is. -->
    <Grid>
        <TextBlock x:Name="TxtLabel" HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="16" Foreground="{DynamicResource SubTextBrush}"/>
    </Grid>
</UserControl>
