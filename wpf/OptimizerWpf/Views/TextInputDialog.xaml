<Window x:Class="OptimizerWpf.Views.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:tr="clr-namespace:OptimizerWpf"
        Title="" Height="170" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource MainBgBrush}">
    <!-- ΝΕΟ - roadmap "Ονομασμένα προφίλ" - απλός, γενικός διάλογος εισαγωγής κειμένου (ζήτησε ένα
         όνομα από τον χρήστη), ίδιο μοτίβο/στυλ με το ήδη υπάρχον AddContextMenuDialog. Δεν υπήρχε
         γενικό "input box" στην εφαρμογή πριν - κάθε προηγούμενη ανάγκη κάλυπτε ένα ήδη υπάρχον, πιο
         συγκεκριμένο παράθυρο. -->
    <StackPanel Margin="16">
        <TextBlock x:Name="TxtPrompt" Style="{StaticResource SubTextStyle}" Margin="0,0,0,8" TextWrapping="Wrap"/>
        <TextBox x:Name="TxtValue" Margin="0,0,0,16" Background="{DynamicResource CardBgBrush}" Foreground="{DynamicResource TextBrush}" KeyDown="TxtValue_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="{tr:Tr Cancel}" Style="{StaticResource FlatButtonStyle}" Width="100" Margin="0,0,10,0" Click="BtnCancel_Click"/>
            <Button x:Name="BtnOk" Content="OK" Style="{StaticResource AccentButtonStyle}" Width="100" Click="BtnOk_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
